#!/bin/bash
set -euo pipefail

# Run mapt with DUMP_FILENAME to create a jsonl file that we can then
# parse to build a SQLite autosuggest db.

DUMP_FILENAME=$(readlink -f objects.jsonl)
export DUMP_FILENAME
rm -f "${DUMP_FILENAME}"
bun mapt build --no-output "$@"
