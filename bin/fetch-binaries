#!/bin/bash
set -euo pipefail

curl --fail --silent https://public.hikeratlas.com/bin/tile-join --output /tmp/tile-join
chmod a+x /tmp/tile-join

/tmp/tile-join

curl --fail --silent https://public.hikeratlas.com/bin/tilemaker --output /tmp/tilemaker
chmod a+x /tmp/tilemaker
/tmp/tilemaker

